pipeline {
    agent any


    stages {
        stage('SCM Checkout'){
          git 'https://github.com/Mukesh889/maven-project.git'
        }
  }
    {
        stage ('Compile Stage') {

            steps {
                withMaven(maven : 'Test_maven') {
                    sh 'mvn clean compile'
                }
            }
        }

        stage ('Testing Stage') {

            steps {
                withMaven(maven : 'Test_maven') {
                    sh 'mvn test'
                }
            }
        }


        stage ('install Stage') {
            steps {
                withMaven(maven : 'Test_maven') {
                    sh 'mvn install'
                }
            }
        }

         
}
}
